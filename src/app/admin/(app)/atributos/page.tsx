import { AdminPagePlaceholder } from "@/components/admin/admin-placeholder";

export default function Page() {
  return (
    <AdminPagePlaceholder
      title="Atributos"
      phase="Fase 4"
      description="Definir atributos dinámicos (Color, Modelo, etc.) y sus valores."
    />
  );
}
